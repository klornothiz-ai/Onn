# ملخص التحسينات الموثّق — Round 26

هذا الملف يلخّص التغييرات التي يمكن إسنادها مباشرة إلى `CHANGES.md` في النسخة الحالية.
تم حذف النصوص التالفة، والمصطلحات المشوّهة، وأي نسب أداء أو أرقام تسويقية لا يثبتها
سجل التغييرات أو اختبار قابل لإعادة الإنتاج.

## Round 26 — إغلاق فجوات مترابطة

تم تنفيذ مجموعة تحسينات مترابطة بدل إضافة packet واحد في كل جولة.

### GPU / GCN

- إضافة نموذج LDS برمجي بسعة **64 KiB** مع فحوصات حدود ونطاق.
- إضافة عمليات DS الأساسية في مسار GCN البرمجي، مع رفض الوصول خارج LDS.
- إضافة:
  - `S_BUFFER_LOAD_DWORD`
  - `S_BUFFER_LOAD_DWORDX2`
  - `S_BUFFER_LOAD_DWORDX4`
- تنفيذ عمليات SMEM الجديدة في كل من الـ compiler والـ software executor باستخدام جدول
  الـ descriptors الموجود، مع التحقق من حجم الـ descriptor.
- تشديد فحوصات `MUBUF` لمنع القراءة أو الكتابة خارج حجم الـ buffer المعلن في الـ descriptor.
- التعامل المحافظ مع `stride == 0` بدل افتراض semantics غير مؤكدة.

### Kernel / File I/O

تم توسيع مسار I/O المربوط بالمضيف، مع التحقق عبر VMM، ليشمل:

- `dup`
- `readv`
- `pipe`
- `ftruncate`

ولا تعود هذه المسارات تُبلغ عن نجاح غير مشروط عندما تفشل العملية الفعلية.

### الاختبارات

أضيف اختباران جديدان:

- `tests/gpu_memory_extended_test.cpp`
- `tests/syscall_io_extended_test.cpp`

وتذكر `CHANGES.md` أن نتيجة التحقق لهذه النسخة هي:

**25/25 CMake/CTest suites pass.**

## ما لا يثبته هذا الملف

هذا الملخص لا يدّعي:

- نسبة مئوية محددة لتحسن الأداء.
- نسبة مئوية محددة لتحسن التغطية.
- عددًا إجماليًا من الاختبارات الداخلية يتجاوز نتيجة CMake/CTest الموثقة.
- توافقًا مع ألعاب PS5 تجارية.
- اكتمال HLE أو RDNA2 أو Vulkan graphics pipeline.

أي أرقام أو ادعاءات من هذا النوع يجب أن تستند إلى benchmark أو اختبار قابل لإعادة الإنتاج،
وإلا فهي ليست جزءًا من هذا الملخص.

## الملفات المرجعية

- `CHANGES.md` — سجل التغييرات الفعلي.
- `docs/SCOPE.md` — نطاق المشروع.
- `docs/HLE_COVERAGE.md` — حالة تغطية HLE.
- `README.md` — وصف المشروع وطريقة البناء والاختبار.

